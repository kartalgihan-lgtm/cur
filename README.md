# DLL project PowerShell demo

This repo contains a simple .NET class library (DLL) and a PowerShell
script that builds and runs it.

## Build and run

From the repo root:

```powershell
pwsh ./DllProject/run.ps1
```

Expected output:

```
Hello, PowerShell from DllProject!
```